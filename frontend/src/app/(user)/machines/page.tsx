import { ComingSoon } from '../../../components/ComingSoon';

export default function MachinesPage() {
  return (
    <ComingSoon
      title="Machines"
      description="Per-machine documentation: programmes, files, lock state, download history."
      legacyRef="frontend/machine/index.blade.php"
    />
  );
}
