import { ComingSoon } from '../../../../../components/ComingSoon';

export default function Page() {
  return (
    <ComingSoon
      title="IoT setup units"
      description="Bind IoT units to equipment, set filter time, assign signal types."
      legacyRef="backend/machine/setupunit.blade.php"
    />
  );
}
