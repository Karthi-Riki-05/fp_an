import { ComingSoon } from '../../../../components/ComingSoon';

export default function Page() {
  return (
    <ComingSoon
      title="Machines (admin)"
      description="IoT machine units, status, configuration."
      legacyRef="backend/machine/machines.blade.php"
    />
  );
}
