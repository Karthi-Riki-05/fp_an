import { ComingSoon } from '../../../../components/ComingSoon';

export default function Page() {
  return (
    <ComingSoon
      title="Feedback (admin)"
      description="Tenant feedback tickets — read, reply, resolve."
      legacyRef="backend/feedback/feedback.blade.php"
    />
  );
}
