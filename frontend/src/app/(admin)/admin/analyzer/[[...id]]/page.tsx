// Admin-shell view of the Flow Analyzer. Same component as `/analyzer` but
// rendered inside the (admin) route group's AdminShell layout. Keeps a
// single source of truth for the page implementation.
export { default } from '../../../../(user)/analyzer/[[...id]]/page';
